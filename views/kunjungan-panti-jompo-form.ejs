<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pendaftaran Panti Jompo - Relawan Nusantara</title>
    <link rel="stylesheet" href="style.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        /* CSS KHUSUS UNTUK HALAMAN INI AGAR TIDAK BLANK */
        body {
            margin: 0;
            padding: 0;
            background-color: #0a0f1e !important; /* Biru gelap pekat sesuai tema */
            font-family: 'Poppins', sans-serif;
            color: white;
            min-height: 100vh;
        }

        /* Navigasi tetap konsisten */
        .navbar {
            background: rgba(10, 15, 30, 0.9);
            padding: 15px 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }

        /* Header yang bikin kesan "Wow" */
        .page-header {
            text-align: center;
            padding: 80px 20px 120px;
            background: linear-gradient(rgba(10, 15, 30, 0.8), rgba(10, 15, 30, 0.9)), 
                        url('https://images.unsplash.com/photo-1516627145497-ae6968895b74?q=80&w=1500');
            background-size: cover;
            background-position: center;
        }

        .page-header h1 {
            font-size: 2.5rem;
            margin: 0;
            letter-spacing: 1px;
        }

        /* Form Container yang Rapi & Presisi */
        .form-wrapper {
            max-width: 550px;
            margin: -70px auto 60px; /* Biar agak naik menimpa header */
            background: #1e293b; /* Warna card sesuai screenshot */
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.5);
            border: 1px solid rgba(255,255,255,0.1);
            position: relative;
        }

        .reward-badge {
            position: absolute;
            top: -15px;
            right: 30px;
            background: #facc15;
            color: #000;
            padding: 5px 15px;
            border-radius: 8px;
            font-weight: 700;
            font-size: 0.8rem;
        }

        .form-header {
            text-align: center;
            margin-bottom: 30px;
        }

        .form-header h2 {
            color: #00d2ff;
            margin: 0;
            font-size: 1.6rem;
        }

        .form-header p {
            font-size: 0.85rem;
            color: #94a3b8;
            margin-top: 5px;
        }

        /* Styling Input */
        .input-box {
            margin-bottom: 20px;
        }

        .input-box label {
            display: block;
            margin-bottom: 8px;
            font-size: 0.85rem;
            color: #cbd5e1;
        }

        .form-control {
            width: 100%;
            padding: 12px 15px;
            background: #0f172a;
            border: 1px solid #334155;
            border-radius: 10px;
            color: white;
            font-family: inherit;
            font-size: 0.9rem;
            box-sizing: border-box;
        }

        .form-control:focus {
            outline: none;
            border-color: #00d2ff;
        }

        /* Baris ganda untuk Usia & HP */
        .form-row {
            display: flex;
            gap: 15px;
        }

        .form-row .input-box {
            flex: 1;
        }

        /* Tombol Utama */
        .btn-kirim {
            width: 100%;
            padding: 15px;
            background: linear-gradient(90deg, #00d2ff, #0077ff);
            border: none;
            border-radius: 10px;
            color: white;
            font-weight: 700;
            font-size: 1rem;
            cursor: pointer;
            transition: 0.3s;
            margin-top: 10px;
        }

        .btn-kirim:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 210, 255, 0.3);
        }

        .checkbox-group {
            display: flex;
            align-items: flex-start;
            gap: 10px;
            background: rgba(255,255,255,0.05);
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .checkbox-group input { margin-top: 3px; }
        .checkbox-group span { font-size: 0.75rem; color: #94a3b8; }

    </style>
</head>
<body>

    <nav class="navbar">
        <div style="display: flex; align-items: center; gap: 20px;">
            <a href="sosial-kemanusiaan.html" style="color: white; text-decoration: none; font-size: 0.9rem;">&larr; Kembali</a>
            <span style="font-weight: 700; color: #00d2ff;">Relawan Nusantara</span>
        </div>
        <div style="color: #facc15; font-weight: 700;" id="display-koin">🪙 0</div>
    </nav>

    <header class="page-header">
        <h1>Aksi Kemanusiaan</h1>
    </header>

    <div class="form-wrapper">
        <div class="reward-badge">+40 KOIN</div>
        <div class="form-header">
            <h2>Kunjungi Panti Jompo</h2>
            <p>Berbagi waktu, menghadirkan senyuman bagi lansia</p>
        </div>

        <form id="pantiForm">
            <div class="input-box">
                <label>Nama Relawan</label>
                <input type="text" class="form-control" placeholder="Nama lengkap Anda" required>
            </div>

            <div class="form-row">
                <div class="input-box">
                    <label>Usia (Min 17)</label>
                    <input type="number" class="form-control" min="17" placeholder="Usia" required>
                </div>
                <div class="input-box">
                    <label>No. WhatsApp</label>
                    <input type="tel" class="form-control" placeholder="0812xxxx" required>
                </div>
            </div>

            <div class="input-box">
                <label>Pilih Jadwal</label>
                <input type="date" class="form-control" required>
            </div>

            <div class="input-box">
                <label>Pilih Aktivitas</label>
                <select class="form-control" required>
                    <option value="">-- Pilih Aktivitas --</option>
                    <option value="ngobrol">Mendengar Cerita & Ngobrol</option>
                    <option value="senam">Senam Lansia Ringan</option>
                    <option value="musik">Bernyanyi & Musik</option>
                    <option value="permainan">Bermain Game Papan</option>
                </select>
            </div>

            <div class="checkbox-group">
                <input type="checkbox" required>
                <span>Saya setuju untuk mengikuti aturan panti dan menjaga kesantunan selama kunjungan berlangsung.</span>
            </div>

            <button type="submit" class="btn-kirim">Daftar & Tebarkan Kebaikan</button>
        </form>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // Urusan Koin
            let koin = parseInt(localStorage.getItem('relawan_koin')) || 0;
            document.getElementById('display-koin').innerText = `🪙 ${koin}`;

            document.getElementById('pantiForm').addEventListener('submit', (e) => {
                e.preventDefault();
                koin += 40;
                localStorage.setItem('relawan_koin', koin);
                
                alert("Pendaftaran Berhasil! 🎉\nKoin Anda bertambah +40.");
                window.location.href = 'sosial-kemanusiaan.html';
            });
        });
    </script>
</body>
</html>